# Brainstorming / design scratch (superpowers visual companion)
.superpowers/

# Impeccable local scratch (sketches, live-mode config) — but the design
# sidecar is the machine-readable half of DESIGN.md and belongs in version
# control, so the detector can check the ship against the recorded system.
.impeccable/*
!.impeccable/design.json

# Local collector state — holds the previous observation the discriminant
# needs. Machine-local by design; must never be committed.
.collector-state/

# macOS
.DS_Store

# Secrets — the local collector reads credentials from ~/.codex/auth.json,
# never from this repo. Nothing token-shaped may ever land here.
.env
.env.*
*.pem
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node / Vercel
node_modules/
.vercel/
.next/
dist/
.vercel
